# NightWatch Chrome Extension

## Usage

> make sure you have node/npm installed

```bash
$ npm install gulp -g
$ npm install bower -g
$ bower install
$ npm install
$ gulp
```

And the compiled chrome extension should be put into a folder called `dest`.
